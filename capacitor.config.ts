import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.jsrtc.blog',
  appName: 'msrtc',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
